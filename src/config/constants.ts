// Local Storage Keys
// Auth tokens (access_token, refresh_token) are stored as httpOnly cookies by the backend
export const LOCAL_STORAGE_KEYS = {
  USER_PROFILE: 'user_profile',
  REMEMBER_ME: 'remember_me',
}
